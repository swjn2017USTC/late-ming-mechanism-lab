# M001 — Fiscal Extraction Inversion

**Question.** Does pressing the fiscal apparatus harder collect less of what it is owed?

**Status.** `CONDITIONAL` (V1) → `CONDITIONAL` (V2) → `CONDITIONAL` (V2.1). V2.1 leaves the status where V2 put it and corrects the card's own sample statement: the factorial is four cells at one seed, each cell one deterministic trajectory, so its four cells are four structures and not four seeds' worth of replicates

## Where the status holds

- **dataset**: historical-core-v1 (toy space for the factorial)
- **forcing**: observed-historical
- **policy**: escalating vs fixed extraction, crossed with the declared price lever
- **sample_size**: 4 arm structures, 4 executions, 4 distinct processes counted by distinct factorial cell: one run per structure, no repeats, 4 independent samples.
- **threshold_band**: not threshold-dependent: the reading reads no governance line
- **window**: whole-run (the protocol's reporting window); no reserved window is read

## Replicate semantics

- **kind**: `one-run-per-structure`
- **independent samples**: 4
- **basis**: 4 arm structures, 4 executions, 4 distinct processes counted by distinct factorial cell: one run per structure, no repeats, 4 independent samples.

## Model evidence

- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.factorial[escalating].inversion_present` = 1
- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.factorial[fixed].inversion_present` = 0
- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.factorial (cells declared)` = 4

## The record

**Support.** `receipts-shortfall-chronic` (grade B, target): receipts below quota persist across the calibration window; the inversion itself is a model-side pattern the record does not name.

**Challenge.** The record documents short receipts and rising assessments separately; no source in the registry measures collection effort against realised receipts, so the inversion's own shape is not attested.

**Counterexample.** Under a fixed-effort policy the inversion is absent in every factorial cell, so a claim that harder pressing always collects less is contradicted inside the model.

**Falsifier.** An arm that fixes collection effort and still shows the inversion would falsify the policy attribution; the fixed cells already fail to show it.

**Uncertainty.** The attribution is a model result on the toy space at 1 seed(s) (20260915); the historical side remains a direction, not a level.

## Lineage

1. **source** — `sources/registry/clusters-06-10.yaml#taxation_levies`: receipt-shortfall and quota-erosion claims, grade B, direction only
2. **normalized** — `data/historical_patterns/07-taxation-levies.yaml#receipts-shortfall-chronic`: the pattern the calibration window scores
3. **parameter-or-rule** — `src/late_ming_lab/policies/fiscal.py#ArrearsEscalation`: the escalating policy the factorial varies
4. **run** — `outputs/v2/p05/p05-factorial-*`: four factorial cells, one seed, historical core scenario
5. **statistic** — `src/late_ming_lab/analysis/mechanisms.py#extraction_inversion`: the inversion reading, threshold-free
6. **card** — `docs/mechanisms/v2_1/cards.yaml#M001`: this card, as V2.1 leaves it

---

Simulation support is not historical truth. The model evidence above is a statement about
this model under a declared configuration; the record supplies direction, sequence and
constraint, and nothing here is a finding about the past.
