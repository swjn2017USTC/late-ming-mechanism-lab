# M005 — 武装整并

**问题** 武装力量是否集中为更少、更大的股,且模型自身的合并链接是否产生之?

**状态** `SUPPORTED` (V1) -> `SUPPORTED` (V2). V1 与 V2 均为 SUPPORTED，且 V2 首次真正触发合并链：合并条归零时最大股占比由 0.24 升至 0.34，条设到上限则完全不合并。

## 结论适用范围

- **dataset**: historical-core-v1
- **forcing**: observed-historical
- **policy**: merge bar at zero, at the declared 0.6, and at its maximum
- **sample_size**: 40 arm runs over 4 seeds; the merge reachability reading covers 223 band-months
- **threshold_band**: not threshold-dependent for the reading; the concentration line is

## 模型证据（非历史事实）

- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.merge_reachability.merge_events` = 582.756
- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.arms[band-merge-negative-control].chains[band_chain_largest_share_end]` = 0.199402
- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.arms[band-merge-positive-control].chains[band_chain_largest_share_end]` = 0.340507
- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.arms[band-merge-negative-control].chains[band_chain_troops.merger]` = 0
- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.arms[band-merge-positive-control].chains[band_chain_troops.merger]` = 4094.26

## 史料部分

**支持** `many-bands-then-consolidation`(B 级,留出)记录多股让位于更少更大的股。

**挑战** 史料描述整合但不描述其成分;究竟是合并还是单纯解散造成,本登记册中没有任何来源加以区分。

**反例** 负对照——门槛拉满——完全不见整合,故整合在危机下并非自动发生。

**可否证预测** 若合并触发而最大份额不上升,链接即为摆设;正对照显示的正好相反。

**不确定性** 一个窗口、一份输入:P10 的夹具未重跑,而正对照达到的份额仍低于一。

## 证据链（source → 规范化行 → 参数/规则 → 运行 → 统计量 → 卡片）

证据链中的定位符与阶段名保留其原语言(路径、哈希与代码标识),以免与英文原稿不一致。

1. **source** -- `sources/registry/clusters-06-10.yaml#rebellion_armed_groups`: band formation, merger and consolidation claims
2. **normalized** -- `data/historical_patterns/10-rebellion-armed-groups.yaml#many-bands-then-consolidation`: the hold-out pattern
3. **parameter-or-rule** -- `src/late_ming_lab/systems/military.py#BandViolenceSystem._merge`: merge_cohesion_above and the co-location rule
4. **run** -- `outputs/v2/p05/p05-band-merge-*`: positive and negative controls at four seeds
5. **statistic** -- `src/late_ming_lab/analysis/band_chain.py`: the six-component attribution of concentration
6. **card** -- `docs/mechanisms/v2/cards.yaml#M005`: this card

---

英文原稿为准；模拟支持不等于历史真相。 (zh-v2.0)
