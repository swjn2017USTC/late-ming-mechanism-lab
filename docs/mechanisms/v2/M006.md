# M006 — Famine Mortality

**Question.** Does excess death from hunger carry a harvest failure into population loss, and from there into labour scarcity and abandoned land?

**Status.** `UNIDENTIFIED` (V1) → `UNIDENTIFIED` (V2). unchanged, and deliberately so: V2 delivered the interface and the observables a mortality rule would consume, and refused to build the rule. The registry's famine cluster says in its own note that no source in it measures a mortality or unmet-need series at county resolution, and the one population claim is grade C with a direction and no parameter. An unidentified mechanism is a result

## Where the status holds

- **dataset**: historical-core-v1
- **forcing**: observed-historical
- **policy**: not applicable: no rule exists to exercise
- **sample_size**: the reference run's cohort months: 16 cohorts short of the floor, longest run 48 months; no arm can bind a mortality rule because none exists
- **threshold_band**: not applicable

## Model evidence

- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.mortality_gap.observables.cohorts_with_a_shortfall` = 16
- `docs/v2/mechanism-variants.json` (6ee117ece3e3) via `variants.mortality_gap.observables.longest_run_below_floor_max` = 48

## The record

**Support.** `famine-lags-harvest-failure` (grade B, constraint) gives the sequence; `pop-ming-qing-decline` (grade C) gives the direction of population loss.

**Challenge.** The sequence's last link — hunger to death — is the part no source here quantifies, and the model reaches abandonment through departure instead.

**Counterexample.** The model satisfies `land-abandonment-in-famine` at 1.00 with zero deaths, so mortality is not necessary for the outcome the card names.

**Falsifier.** A sourced county-resolution mortality series with a level would let a rule be declared; until then no arm can bind and none is claimed.

**Uncertainty.** Everything about the level: the observables exist (16 of 60 cohorts short, longest run 48 months, rolling ratio 0.224) and nothing outside the model exists to calibrate a rate against.

## Lineage

1. **source** — `data/normalized/evidence_ledger-01-05.yaml#pop-ming-qing-decline`: grade C, direction only, supports no parameter
2. **normalized** — `data/historical_patterns/02-famine.yaml`: the cluster note that states no mortality series exists
3. **parameter-or-rule** — `src/late_ming_lab/systems/mortality.py`: no rate: mortality_rule_from_evidence refuses to build one
4. **run** — `outputs/v2/p05/p05-reference-*`: the reference run, from which the observables are recomputed
5. **statistic** — `src/late_ming_lab/analysis/mortality.py#mortality_gap_summary`: cohorts short, longest run, rolling ratio, deaths = 0
6. **card** — `docs/mechanisms/v2/cards.yaml#M006`: this card

---

Simulation support is not historical truth. The model evidence above is a statement about
this model under a declared configuration; the record supplies direction, sequence and
constraint, and nothing here is a finding about the past.
