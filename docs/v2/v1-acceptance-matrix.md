# V1 acceptance audit

Generated by `python -m late_ming_lab.release audit` from `docs/v2/baseline-v1.json` and `docs/mechanisms/cards.yaml`. Do not edit by hand.

- Baseline: `v0.1.0-rc1`, commit `a81c223216f989150d69fe42bfe0a78f7eab1374`
- Baseline content digest: `c8e34e316c827cea5477aa3fc4829fb1d2ce47f1076a9a95347a52d15f1f49b8`
- Audit content digest: `34c6ebec352d54d3f14827db364088df6bb9c3a1b6bd843a8347922acffc7941`
- Generated: 2026-09-14T10:12:40.297858+00:00

Statuses: `met` (the criterion is satisfied by evidence in the release), `partial` (part of it holds and part does not), `unmet` (the evidence contradicts it), `not-testable` (no artifact in the release can decide it).

## 1. The sandbox this audit is about

5 county nodes and 3 external nodes (8 nodes), 25 weighted household cohorts covering 21,000 households, 5 elite houses, 7 merchant houses, 5 county governments, 5 garrison units and 0 armed bands at start.

- Space: built by `src/late_ming_lab/networks/fixtures.py:toy_spatial_dataset`, `src/late_ming_lab/actors/fixtures.py:toy_cohort_population` and `src/late_ming_lab/experiments/integrated.py:build_integrated_economy`
- Climate: synthetic stationary: a shock month is drawn independently with probability 0.4 and severity floor 0.6; no observed series and no trend (`src/late_ming_lab/systems/climate.py:SyntheticClimate`)
- Scenario knobs: `src/late_ming_lab/experiments/integrated.py:IntegratedScenario`

## 2. Milestones

| id | milestone | status | verdict |
| --- | --- | --- | --- |
| M1 | Mechanism-capable deterministic model | **partial** | The LLM-free half holds and the triple test exists. What does not hold is the first half: the ensemble is a 32-particle pipeline rather than a posterior, two of thirteen parameters are unpinned across sampler seeds, and the hold-out window does not produce several independent patterns - three of the eight reserved patterns are contradicted by the model's own recorded falsifier, and the contradiction traces to a stationary synthetic climate, so those claims are untestable in this model rather than wrong in it. |
| M2 | Policy-robust mechanism model | **partial** | Not met as stated, because the arm the milestone names by model never ran: the P11 gate refused the configured id and P12 recorded the refusal rather than imputing a result. What does hold is narrower: with the world, the parameters and the random numbers held fixed, armed-band consolidation appears in five of six replicates under each of the three declared offline policies, extraction inversion reaches the declared presence bar under exactly one of them (utility 6 of 6 replicates, against random 2 of 6 and rule 0 of 6), and the tipping region moves with the policy. No statement is made about a model-backed policy. |

### M1 - Mechanism-capable deterministic model

**Requirement.** Without any LLM, a calibrated multilayer ABM produces several independent historical patterns in the hold-out window, and for at least one candidate mechanism the sensitivity, ablation and counterfactual tests have all been done.

**Status: partial.** The LLM-free half holds and the triple test exists. What does not hold is the first half: the ensemble is a 32-particle pipeline rather than a posterior, two of thirteen parameters are unpinned across sampler seeds, and the hold-out window does not produce several independent patterns - three of the eight reserved patterns are contradicted by the model's own recorded falsifier, and the contradiction traces to a stationary synthetic climate, so those claims are untestable in this model rather than wrong in it.

**Evidence.**

- `docs/calibration/prediction.md` - document: the reserved-pattern table - three of eight reserved patterns fail their own falsifier over the run
- `docs/calibration/stability.md` - document: seed comparison - two of thirteen parameters move by over a quarter of their prior range
- `docs/calibration/posterior.md` - document: identifiability table - one SMC batch per seed: a particle set, not a posterior
- `outputs/experiments/p10-ablations/runs.parquet` - artifact: 52 rows; arm OPEN_MIGRATION_EXIT - the mechanism-side intervention: breakdown in 4 of 4 replicates
- `outputs/experiments/p10-morris/runs.parquet` - artifact: 32 rows, 2 trajectories - the sensitivity screen behind the same card
- `outputs/experiments/p12-robustness/manifest.json` - artifact: arm_status - three declared policies ran; the runtime arm is refused, so no LLM decided anything

### M2 - Policy-robust mechanism model

**Requirement.** Core mechanisms are robust across several institutional decision models: rule, utility and the USTC DeepSeek V4.1 runtime policy.

**Status: partial.** Not met as stated, because the arm the milestone names by model never ran: the P11 gate refused the configured id and P12 recorded the refusal rather than imputing a result. What does hold is narrower: with the world, the parameters and the random numbers held fixed, armed-band consolidation appears in five of six replicates under each of the three declared offline policies, extraction inversion reaches the declared presence bar under exactly one of them (utility 6 of 6 replicates, against random 2 of 6 and rule 0 of 6), and the tipping region moves with the policy. No statement is made about a model-backed policy.

**Evidence.**

- `docs/experiments/policy-robustness.md` - document: the matrix and its verdict rules - consolidation robust at three arms; inversion policy-dependent; ratchet absent
- `outputs/experiments/p12-robustness/manifest.json` - artifact: arm_status.ustc - status refused, 0 decisions, with the gate's reason recorded
- `outputs/experiments/p12-robustness/mechanism_readings.parquet` - artifact: 54 rows = 18 runs x 3 mechanisms - the per-run readings the matrix is the share of
- `docs/architecture/runtime-llm-boundary.md` - document: the fail-closed contract - one confirmed model id or nothing; no fallback, recording and offline replay

## 3. Final success criteria

| # | criterion | status | verdict |
| --- | --- | --- | --- |
| 1 | every key rule has provenance | **met** | Twenty-four declared rules each carry a support class and the claims that bear on them, and the referential chain is enforced in both directions by the invariant suite - which is how two claims pointing at rule names the code does not have were caught and recorded in the phase that built the registry. The weaker half is what the provenance is worth: eleven rules are evidence-backed, seven theoretically assumed and six exploratory, and thirteen of twenty-five sources were never opened. |
| 2 | multi-scale historical patterns are explained together | **unmet** | Not achieved, and measured so. Five target patterns are scored in the calibration window: two are satisfied by every posterior draw, one by 0.81, and two are systematically unsatisfied on their own checks. Of the eight reserved patterns, three are contradicted with the pattern's own falsifier as the failure (chongzhen-drought-sequence 0.00, famine-worst-years-1639-43 0.00, price-spike-concentration 0.44 over the whole run), four are satisfied (many-bands-then-consolidation 0.94, pay-monetised-and-arrears 1.00, quota-erosion-and-surcharge 1.00, relief-overwhelmed-in-worst-years 0.84), and one passes on timing while the quantity it measures is near zero (shaanxi-net-outflow, whose exits are zero inside the hold-out window). |
| 3 | calibration and hold-out are separated | **met** | Enforced structurally rather than by convention: the objective refuses every window but the calibration window, the predictive checks refuse the calibration window, and a test runs a real sandbox run, deletes every event after the split, and asserts the score does not move. The reserved patterns were fixed in P08, before any of them was scored. |
| 4 | model results are explicitly sensitive to parameter perturbation | **partial** | A ranking exists and is readable on the migration output; the sensitivity claim is not yet a quantitative one. The Morris screen is two trajectories over fifteen parameters, so most rankings rest on one or two elementary effects, and the Sobol design at four base samples returns four undefined first-order indices and nine of the twelve finite ones outside [0, 1] - the phase reports it as a failed attempt rather than as an estimate. |
| 5 | real interventions are possible | **met** | Thirteen arms - one declared baseline, nine ablations and three joint arms - are computed differences from that baseline under common random numbers, and the reports carry the counters that show whether an arm bound: elite loans 74 to 0, suppressions 936.5 to 0. Paired intervals, direction stability and Cliff's delta are reported rather than a mean, so an unresolved arm is visible as unresolved. |
| 6 | key mechanisms can be removed by ablation | **partial** | For the mechanisms that have a removable rule, yes: removing elite credit and suppression both bind and both move the outputs their mechanism reaches. For three of the six cards, no arm can bind at all - the band-merger rule produced zero merges anywhere in the batch, so its ablation is a structural no-op; elite accumulation was never built, so the bifurcation has no second branch to remove; and famine mortality has no rule, parameter or output to delete. |
| 7 | the parameter regions in which a mechanism holds can be given | **partial** | One region is published and it is a cell, not a surface: the P10 grid is three by three at one run per cell, and exactly one cell breaks down, which is one run rather than a probability. The P12 grid places a region per policy at two replicates per cell and shows it moving with the policy. The parameter-side boundary is refused rather than forced, because the five-fold split cannot be scored at this sample size. |
| 8 | the tipping surface can be identified | **partial** | A surface can be fitted and its limits are measured: on the P12 grid the cross-validated AUC is 0.754 against an in-sample 0.723 over 54 runs, but per policy the random arm gives 0.550 with a spread of 0.400, which separates almost nothing. The P10 grid cannot resolve a region at one run per cell, and its parameter-side fit is refused, so the identification is partial at 18 to 54 rows per surface. |
| 9 | historical counterexamples can be listed | **met** | Every card carries a counterexample field, and they are real ones: the rule policy shows no extraction inversion at the same parameter draws; the ratchet's strongest replicate still declines in 37 months; consolidation appears unchanged when the merger rule it names is removed. The review of the phase that wrote them found no fabricated number and rewrote the prose that reached past its artifact: a counterexample that cited the wrong replicate, a status moved from CONDITIONAL to WEAK, and a sensitivity ranking that named parameters the card's own table does not rank first. |
| 10 | new predictions that return to the historical sources can be proposed | **met** | Each card carries a falsifiable prediction naming the arm or observation that would refute it, and several are already decisive: shut the gate and breakdown should not occur; fix collection effort and the inversion should disappear. They are predictions rather than results - the review records that the arms they describe do not exist yet, which is what makes them predictions. |
| 11 | LLM decisions do not become an unexplainable black box | **partial** | By construction the layer is inspectable: a bounded action space per role, an anonymized observation with no date, place or person, Pydantic-validated output, an inert rationale, a recorded prompt and response hash, and no tools - all enforced offline by tests, including one that drives a hostile rationale and gets the benign world effect. It is also untested at runtime, because zero live decisions exist: the gate is closed, the recorded corpus is empty, and the two decisions in the smoke trace were made by the declared rule policy. |
| 12 | any result can be replayed from its run manifest | **partial** | The provenance machinery exists and one end-to-end case is proven: the declared demo reproduces the digest recorded in its scenario file. Three gaps are measured in this phase's own freeze. parameter_hash is null in every run manifest, because the kernel does not know the parameter sets. Seven of the nine artifact families with a manifest were generated from a dirty tree, so the recorded commit does not fully determine their code. And outputs/analysis carries nine Parquet files with no manifest at all, so those results have no recorded generating commit to replay from. |

### Criterion 1 - every key rule has provenance

**Status: met.** Twenty-four declared rules each carry a support class and the claims that bear on them, and the referential chain is enforced in both directions by the invariant suite - which is how two claims pointing at rule names the code does not have were caught and recorded in the phase that built the registry. The weaker half is what the provenance is worth: eleven rules are evidence-backed, seven theoretically assumed and six exploratory, and thirteen of twenty-five sources were never opened.

**Evidence.**

- `data/normalized/rule_claims.yaml` - registry: 24 rule records - each rule with its support class and citing claims
- `tests/invariants/test_evidence_invariants.py` - code: claims to rules and rules to claims - the reverse-direction check that caught the non-canonical rule names
- `docs/phase-reports/P08.md` - document: what this phase found (and fixed), items 1 to 3 - the eighteen citations trimmed and the two rule names that do not exist
- `docs/evidence/coverage.md` - document: coverage by cluster - sources, claims and patterns per cluster, with the layer mix
- `docs/evidence/gaps.md` - document: the acquisition queue - what could not be verified openly and is left to a human

### Criterion 2 - multi-scale historical patterns are explained together

**Status: unmet.** Not achieved, and measured so. Five target patterns are scored in the calibration window: two are satisfied by every posterior draw, one by 0.81, and two are systematically unsatisfied on their own checks. Of the eight reserved patterns, three are contradicted with the pattern's own falsifier as the failure (chongzhen-drought-sequence 0.00, famine-worst-years-1639-43 0.00, price-spike-concentration 0.44 over the whole run), four are satisfied (many-bands-then-consolidation 0.94, pay-monetised-and-arrears 1.00, quota-erosion-and-surcharge 1.00, relief-overwhelmed-in-worst-years 0.84), and one passes on timing while the quantity it measures is near zero (shaanxi-net-outflow, whose exits are zero inside the hold-out window).

**Evidence.**

- `docs/calibration/mismatch.md` - document: failing checks by pattern - which target checks fail and in what share of draws
- `outputs/calibration/p09-*/predictive_checks.parquet` - artifact: reserved-pattern checks - the hold-out scores the failure shares are computed from
- `docs/calibration/prediction.md` - document: what the ensemble fails at - the three contradicted reserved patterns and the reason they are contradicted

### Criterion 3 - calibration and hold-out are separated

**Status: met.** Enforced structurally rather than by convention: the objective refuses every window but the calibration window, the predictive checks refuse the calibration window, and a test runs a real sandbox run, deletes every event after the split, and asserts the score does not move. The reserved patterns were fixed in P08, before any of them was scored.

**Evidence.**

- `src/late_ming_lab/calibration/windows.py` - code: the three-way split - calibration / hold-out / extrapolation, tiling the run with no gap
- `src/late_ming_lab/calibration/targets.py` - code: Objective.score - refuses any window but the calibration window
- `tests/invariants/test_calibration_invariants.py` - code: blindness test - the score does not move when post-split events are deleted
- `docs/calibration/objective.md` - document: the frozen objective - frozen bounds, checks and registry digest

### Criterion 4 - model results are explicitly sensitive to parameter perturbation

**Status: partial.** A ranking exists and is readable on the migration output; the sensitivity claim is not yet a quantitative one. The Morris screen is two trajectories over fifteen parameters, so most rankings rest on one or two elementary effects, and the Sobol design at four base samples returns four undefined first-order indices and nine of the twelve finite ones outside [0, 1] - the phase reports it as a failed attempt rather than as an estimate.

**Evidence.**

- `docs/experiments/sensitivity.md` - document: Morris table and Sobol verdict - the ranking, the NaN indices and the out-of-range values, with intervals
- `outputs/experiments/p10-morris/runs.parquet` - artifact: 32 rows, 2 trajectories x 15 parameters - the design the screen is computed from
- `outputs/experiments/p10-sobol/runs.parquet` - artifact: 40 rows, base N=4 - the Sobol design that cannot estimate a variance share at this size

### Criterion 5 - real interventions are possible

**Status: met.** Thirteen arms - one declared baseline, nine ablations and three joint arms - are computed differences from that baseline under common random numbers, and the reports carry the counters that show whether an arm bound: elite loans 74 to 0, suppressions 936.5 to 0. Paired intervals, direction stability and Cliff's delta are reported rather than a mean, so an unresolved arm is visible as unresolved.

**Evidence.**

- `src/late_ming_lab/experiments/interventions.py` - code: BASELINE and the arm functions - every arm is a function of one declared baseline
- `docs/experiments/ablation.md` - document: the binding table - a counter per mechanism, so a no-op arm is reported as a no-op
- `outputs/experiments/p10-ablations/runs.parquet` - artifact: 52 rows = 13 arms x 4 replicates - the runs the paired differences are computed from

### Criterion 6 - key mechanisms can be removed by ablation

**Status: partial.** For the mechanisms that have a removable rule, yes: removing elite credit and suppression both bind and both move the outputs their mechanism reaches. For three of the six cards, no arm can bind at all - the band-merger rule produced zero merges anywhere in the batch, so its ablation is a structural no-op; elite accumulation was never built, so the bifurcation has no second branch to remove; and famine mortality has no rule, parameter or output to delete.

**Evidence.**

- `docs/experiments/ablation.md` - document: did the ablations actually bind - band_merges 0 against 0: nothing to remove
- `docs/mechanisms/M004-elite-mediation-bifurcation.md` - document: ablation evidence - the accumulating branch has no arm to remove
- `docs/mechanisms/M006-famine-mortality.md` - document: ablation evidence - no mortality rule exists, so removing it would be a no-op by construction

### Criterion 7 - the parameter regions in which a mechanism holds can be given

**Status: partial.** One region is published and it is a cell, not a surface: the P10 grid is three by three at one run per cell, and exactly one cell breaks down, which is one run rather than a probability. The P12 grid places a region per policy at two replicates per cell and shows it moving with the policy. The parameter-side boundary is refused rather than forced, because the five-fold split cannot be scored at this sample size.

**Evidence.**

- `docs/experiments/tipping.md` - document: the grid - one breakdown cell at one run per cell
- `docs/experiments/policy-robustness.md` - document: cells with breakdown, per policy - the region per arm at two replicates per cell
- `outputs/reports/mechanism-synthesis.md` - document: section 4.3 - the parameter-side fit is refused, and says why

### Criterion 8 - the tipping surface can be identified

**Status: partial.** A surface can be fitted and its limits are measured: on the P12 grid the cross-validated AUC is 0.754 against an in-sample 0.723 over 54 runs, but per policy the random arm gives 0.550 with a spread of 0.400, which separates almost nothing. The P10 grid cannot resolve a region at one run per cell, and its parameter-side fit is refused, so the identification is partial at 18 to 54 rows per surface.

**Evidence.**

- `outputs/reports/mechanism-synthesis.md` - document: sections 4.1 to 4.4 - cross-validated fits, per-policy coefficients, and what they do not decide
- `outputs/experiments/p12-robustness/tipping_grid.parquet` - artifact: the grid rows - the runs the scenario surface is fitted on
- `src/late_ming_lab/analysis/boundary.py` - code: the refusal on a single-class fold - a fit the table cannot carry is refused, not forced

### Criterion 9 - historical counterexamples can be listed

**Status: met.** Every card carries a counterexample field, and they are real ones: the rule policy shows no extraction inversion at the same parameter draws; the ratchet's strongest replicate still declines in 37 months; consolidation appears unchanged when the merger rule it names is removed. The review of the phase that wrote them found no fabricated number and rewrote the prose that reached past its artifact: a counterexample that cited the wrong replicate, a status moved from CONDITIONAL to WEAK, and a sensitivity ranking that named parameters the card's own table does not rank first.

**Evidence.**

- `docs/mechanisms/cards.yaml` - registry: the counterexample field of all six cards - one recorded counterexample per card, checked in review
- `docs/phase-reports/P13.md` - document: the review findings table - the counterexample and figure corrections the review forced
- `outputs/experiments/p12-robustness/mechanism_readings.parquet` - artifact: presence per run and policy - the measurement the inversion counterexample rests on

### Criterion 10 - new predictions that return to the historical sources can be proposed

**Status: met.** Each card carries a falsifiable prediction naming the arm or observation that would refute it, and several are already decisive: shut the gate and breakdown should not occur; fix collection effort and the inversion should disappear. They are predictions rather than results - the review records that the arms they describe do not exist yet, which is what makes them predictions.

**Evidence.**

- `docs/mechanisms/cards.yaml` - registry: the falsifiable_prediction field of all six cards - six predictions, each naming the measurement that would refute the card
- `data/historical_patterns` - registry: 22 patterns with signatures and falsifiers - the observations that would count against the model, recorded before scoring
- `docs/evidence/gaps.md` - document: the acquisition queue - which sources would discriminate between the surviving candidates

### Criterion 11 - LLM decisions do not become an unexplainable black box

**Status: partial.** By construction the layer is inspectable: a bounded action space per role, an anonymized observation with no date, place or person, Pydantic-validated output, an inert rationale, a recorded prompt and response hash, and no tools - all enforced offline by tests, including one that drives a hostile rationale and gets the benign world effect. It is also untested at runtime, because zero live decisions exist: the gate is closed, the recorded corpus is empty, and the two decisions in the smoke trace were made by the declared rule policy.

**Evidence.**

- `docs/architecture/runtime-llm-boundary.md` - document: the sixteen requirements - where each requirement is enforced, and that the live model is closed
- `tests/policies/test_live_ustc.py` - code: the refusal - a closed gate fails rather than skipping, so it cannot read as green
- `outputs/institutional/institutional-smoke-20260915-d7f738698fbc-institutional-smoke/decision_trace.parquet` - artifact: 2 decision rows - prompt and response hashes recorded, made by policy rule-v1, not a model

### Criterion 12 - any result can be replayed from its run manifest

**Status: partial.** The provenance machinery exists and one end-to-end case is proven: the declared demo reproduces the digest recorded in its scenario file. Three gaps are measured in this phase's own freeze. parameter_hash is null in every run manifest, because the kernel does not know the parameter sets. Seven of the nine artifact families with a manifest were generated from a dirty tree, so the recorded commit does not fully determine their code. And outputs/analysis carries nine Parquet files with no manifest at all, so those results have no recorded generating commit to replay from.

**Evidence.**

- `docs/v2/baseline-v1.json` - artifact: artifacts[].generation_commit, git_dirty_at_generation - what commit each artifact recorded, and whether the tree was dirty
- `outputs/runs/kernel-smoke-20260912-9153567ed602-p05-pressure-0.16/manifest.json` - artifact: parameter_hash - null, with config hash, subsystem seeds and tick order present
- `docs/phase-reports/P09.md` - document: what this phase found (and fixed), item 6 - the parameter_hash gap, recorded and left open by the phase that found it
- `data/scenarios/demo.yaml` - registry: the recorded digest - the one reproducibility claim a test exercises end to end

## 4. Mechanism cards: dataset, sample size, policy arms, extrapolation limits

| card | status | datasets | sample size | policy arms |
| --- | --- | --- | --- | --- |
| M001 | CONDITIONAL | `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`, `outputs/experiments/p10-morris`, `outputs/experiments/p12-robustness` | `p09-cards87a14e9d-32p-ch1-s20260913`: 32 rows in ensemble.parquet; `p09-cards87a14e9d-32p-ch1-s991753`: 32 rows in ensemble.parquet; `p10-ablations`: 52 rows in runs.parquet; `p10-morris`: 32 rows in runs.parquet; `p12-robustness`: 18 rows in levels.parquet | p10-ablations: 13 declared arms, 4 replicates each; p10-morris: morris design; p12-robustness: rule,utility,random,ustc; refused: ustc |
| M002 | SUPPORTED | `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`, `outputs/experiments/p10-morris`, `outputs/experiments/p12-robustness` | `p09-cards87a14e9d-32p-ch1-s20260913`: 32 rows in ensemble.parquet; `p09-cards87a14e9d-32p-ch1-s991753`: 32 rows in ensemble.parquet; `p10-ablations`: 52 rows in runs.parquet; `p10-morris`: 32 rows in runs.parquet; `p12-robustness`: 18 rows in levels.parquet | p10-ablations: 13 declared arms, 4 replicates each; p10-morris: morris design; p12-robustness: rule,utility,random,ustc; refused: ustc |
| M003 | REJECTED | `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`, `outputs/experiments/p12-robustness` | `p09-cards87a14e9d-32p-ch1-s20260913`: 32 rows in ensemble.parquet; `p09-cards87a14e9d-32p-ch1-s991753`: 32 rows in ensemble.parquet; `p10-ablations`: 52 rows in runs.parquet; `p12-robustness`: 18 rows in levels.parquet | p10-ablations: 13 declared arms, 4 replicates each; p12-robustness: rule,utility,random,ustc; refused: ustc |
| M004 | WEAK | `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations` | `p09-cards87a14e9d-32p-ch1-s20260913`: 32 rows in ensemble.parquet; `p09-cards87a14e9d-32p-ch1-s991753`: 32 rows in ensemble.parquet; `p10-ablations`: 52 rows in runs.parquet | p10-ablations: 13 declared arms, 4 replicates each |
| M005 | SUPPORTED | `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`, `outputs/experiments/p10-morris`, `outputs/experiments/p12-robustness` | `p09-cards87a14e9d-32p-ch1-s20260913`: 32 rows in ensemble.parquet; `p09-cards87a14e9d-32p-ch1-s991753`: 32 rows in ensemble.parquet; `p10-ablations`: 52 rows in runs.parquet; `p10-morris`: 32 rows in runs.parquet; `p12-robustness`: 18 rows in levels.parquet | p10-ablations: 13 declared arms, 4 replicates each; p10-morris: morris design; p12-robustness: rule,utility,random,ustc; refused: ustc |
| M006 | UNIDENTIFIED | none | none | no batch and no policy arm: the card cites the record, and nothing the project ran measures this mechanism |

### M001 - Fiscal Extraction Inversion (CONDITIONAL)

- **Dataset.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`, `outputs/experiments/p10-morris`, `outputs/experiments/p12-robustness`
- **Sample size.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/experiments/p10-ablations` (52 rows in runs.parquet, 240 ticks, 20260914 seed, 13 declared arms, generated at 72aa3f92bc95); `outputs/experiments/p10-morris` (32 rows in runs.parquet, 240 ticks, 20260914 seed, 32 declared arms, generated at 72aa3f92bc95); `outputs/experiments/p12-robustness` (18 rows in levels.parquet, 6 replicates, 240 ticks, 20260916 seed, 4 policy arms, generated at c1497d39a771)
- **Space.** 5 county nodes and 3 external nodes (8 nodes), 25 weighted household cohorts covering 21,000 households, 5 elite houses, 7 merchant houses, 5 county governments, 5 garrison units and 0 armed bands at start; the sandbox is a declared fixture graded S, not a reconstruction
- **Climate.** synthetic stationary: a shock month is drawn independently with probability 0.4 and severity floor 0.6; no observed series and no trend - every batch this card cites ran on it, so no claim in the card is about observed climate
- **Policy arms.** p10-ablations: 13 declared arms, 4 replicates each; p10-morris: morris design; p12-robustness: rule,utility,random,ustc; refused: ustc
- **Extrapolation limits.** Three declared policies, three counties, thirty-two cohorts. The utility policy's ceiling of 5.0 is a declared bound, not an observed one, and the inversion's dependence on that ceiling is untested. The one arm that varies extraction runs the other way, so the card rests on a within-run co-movement under a single policy.
- **Counterexample.** The rule policy: 0 of 6 replicates, at the same parameter draws and the same world as the arm that shows it. A structural inversion would not depend on which declared policy is deciding.
- **Evidence.**

  - `outputs/experiments/p10-morris/runs.parquet` - artifact: sensitivity citation on M001 - assessed_value_tael_per_mu mu*=0.75 sigma=1.06 on indicators_crossed_end
  - `outputs/calibration/p09-*/ensemble.parquet` - artifact: calibration citation on M001 - assessed_value_tael_per_mu median 0.240, contraction 0.401, identified
  - `outputs/experiments/p10-ablations/runs.parquet` - artifact: ablation citation on M001 - receipts_over_quota_total -0.06 [-0.10, -0.01] down
  - `outputs/calibration/p09-*/predictive_checks.parquet` - artifact: hold-out citation on M001 - quota-erosion-and-surcharge held-out 1.00, whole run 1.00
  - `outputs/experiments/p12-robustness/mechanism_readings.parquet` - artifact: policy-robustness citation on M001 - inversion present in 1.00/utility, 0.33/random, 0.00/rule; verdict policy-dependent
  - `ray-huang-1974` - registry: historical-support citation on M001 - receipts-shortfall-chronic (grade B, target, window 1600-1644): calibration window 1.00

### M002 - Crisis Gating (SUPPORTED)

- **Dataset.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`, `outputs/experiments/p10-morris`, `outputs/experiments/p12-robustness`
- **Sample size.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/experiments/p10-ablations` (52 rows in runs.parquet, 240 ticks, 20260914 seed, 13 declared arms, generated at 72aa3f92bc95); `outputs/experiments/p10-morris` (32 rows in runs.parquet, 240 ticks, 20260914 seed, 32 declared arms, generated at 72aa3f92bc95); `outputs/experiments/p12-robustness` (18 rows in levels.parquet, 6 replicates, 240 ticks, 20260916 seed, 4 policy arms, generated at c1497d39a771)
- **Space.** 5 county nodes and 3 external nodes (8 nodes), 25 weighted household cohorts covering 21,000 households, 5 elite houses, 7 merchant houses, 5 county governments, 5 garrison units and 0 armed bands at start; the sandbox is a declared fixture graded S, not a reconstruction
- **Climate.** synthetic stationary: a shock month is drawn independently with probability 0.4 and severity floor 0.6; no observed series and no trend - every batch this card cites ran on it, so no claim in the card is about observed climate
- **Policy arms.** p10-ablations: 13 declared arms, 4 replicates each; p10-morris: morris design; p12-robustness: rule,utility,random,ustc; refused: ustc
- **Extrapolation limits.** One gate was intervened on. The relief and trade channels have arms whose effects on the crossed-line count are unresolved at four replicates, so the card does not claim they are not gates. Three counties, thirty-two cohorts, and a movement rule whose distress lines are declared rather than observed.
- **Counterexample.** The drought ablation: removing the climate shock keeps the baseline outcome - no breakdown in any replicate - so the shock is not the gate. Of the twelve arms in the ablation batch only the gate arm breaks down. Elsewhere in P10 breakdown does occur without it - 7 of the 40 Sobol runs and one tipping cell cross the line under extreme parameter draws - so the claim is about the declared arms, not the only route.
- **Evidence.**

  - `outputs/experiments/p10-ablations/runs.parquet` - artifact: ablation citation on M002 - OPEN_MIGRATION_EXIT breakdown +1.00 [+1.00, +1.00] up; NO_DROUGHT breakdown +0.00 [+0.00, +0.00] unresolved
  - `outputs/experiments/p10-ablations/runs.parquet` - artifact: counterfactual citation on M002 - NO_DROUGHT x FULL_MILITARY_PAY on indicators_crossed_end: joint +0.00 against -0.50 additive [+0.00, +1.00], additive
  - `outputs/experiments/p10-morris/runs.parquet` - artifact: sensitivity citation on M002 - strongest parameters on indicators_crossed_end: temporary_share_of_adults_per_month, reference_price_tael_per_shi, land_per_adult_capacity_mu
  - `outputs/experiments/p12-robustness/tipping_grid.parquet` - artifact: policy-robustness citation on M002 - cells with breakdown: rule 7, random 6, utility 5
  - `outputs/calibration/p09-*/predictive_checks.parquet` - artifact: hold-out citation on M002 - shaanxi-net-outflow: held-out 0.16, extrapolation 0.00, whole run 1.00
  - `data/historical_patterns/01-drought-climate.yaml` - registry: historical-challenge citation on M002 - chongzhen-drought-sequence (grade B, hold-out): held-out years 0.00 - a refuted check, cited here as a challenge rather than as support

### M003 - Fiscal-Military Ratchet (REJECTED)

- **Dataset.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`, `outputs/experiments/p12-robustness`
- **Sample size.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/experiments/p10-ablations` (52 rows in runs.parquet, 240 ticks, 20260914 seed, 13 declared arms, generated at 72aa3f92bc95); `outputs/experiments/p12-robustness` (18 rows in levels.parquet, 6 replicates, 240 ticks, 20260916 seed, 4 policy arms, generated at c1497d39a771)
- **Space.** 5 county nodes and 3 external nodes (8 nodes), 25 weighted household cohorts covering 21,000 households, 5 elite houses, 7 merchant houses, 5 county governments, 5 garrison units and 0 armed bands at start; the sandbox is a declared fixture graded S, not a reconstruction
- **Climate.** synthetic stationary: a shock month is drawn independently with probability 0.4 and severity floor 0.6; no observed series and no trend - every batch this card cites ran on it, so no claim in the card is about observed climate
- **Policy arms.** p10-ablations: 13 declared arms, 4 replicates each; p12-robustness: rule,utility,random,ustc; refused: ustc
- **Extrapolation limits.** The rejection is of the strong claim only, at three counties and 240 ticks. The mechanism behind the declines is not identified: nothing measures whether they come from the payment rule, from re-assessment, or from the extraction arm's own response to the stock. The level claim rests on a single held-out pattern at grade A.
- **Counterexample.** arrears 254 to 26,849 tael with 37 month(s) of decline, against 287,355 assessed: the rule arm's largest end stock belongs to a replicate that still records dozens of months in which the stock falls, and the arm's largest decline count is 53. Any reading that needs the stock to be monotone is false in this model.
- **Evidence.**

  - `outputs/experiments/p12-robustness/mechanism_readings.parquet` - artifact: policy-robustness citation on M003 - ratchet present in 0 of 3 declared policies; decline months per replicate [11, 35, 37, 43, 45, 53]
  - `outputs/experiments/p10-ablations/runs.parquet` - artifact: ablation citation on M003 - FULL_MILITARY_PAY military_pay_arrears_end_tael +2,299.70 [+1,554.58, +4,753.25] up
  - `outputs/experiments/p10-ablations/runs.parquet` - artifact: ablation citation on M003 - OPEN_MIGRATION_EXIT military_pay_arrears_max_tael +10,287.79 [+7,289.76, +14,415.10] up
  - `outputs/calibration/p09-*/ensemble.parquet` - artifact: calibration citation on M003 - pay_tael_per_soldier_month median 0.508, contraction 0.271
  - `ray-huang-1974` - registry: historical-support citation on M003 - pay-monetised-and-arrears (grade A, hold-out, window 1580-1644): held-out years 1.00

### M004 - Elite Mediation Bifurcation (WEAK)

- **Dataset.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`
- **Sample size.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/experiments/p10-ablations` (52 rows in runs.parquet, 240 ticks, 20260914 seed, 13 declared arms, generated at 72aa3f92bc95)
- **Space.** 5 county nodes and 3 external nodes (8 nodes), 25 weighted household cohorts covering 21,000 households, 5 elite houses, 7 merchant houses, 5 county governments, 5 garrison units and 0 armed bands at start; the sandbox is a declared fixture graded S, not a reconstruction
- **Climate.** synthetic stationary: a shock month is drawn independently with probability 0.4 and severity floor 0.6; no observed series and no trend - every batch this card cites ran on it, so no claim in the card is about observed climate
- **Policy arms.** p10-ablations: 13 declared arms, 4 replicates each
- **Extrapolation limits.** The card is named for a bifurcation and the evidence resolves one branch of it, which is why the status is WEAK rather than CONDITIONAL: credit is load-bearing, and the claim that elites divide into mediators and accumulators with different consequences is neither measured nor consistent with the runs, where both sides appear together. The credit channel is also entangled with migration, so part of what looks like a credit effect on the base may be a movement effect, and the parameter that pins it is identified against the land-transfer target the mechanism itself implies.
- **Counterexample.** The baseline runs satisfy debt-transfers-land at 0.94 while the tax base contracts by 15,476 mu, with land moving to creditors as the pattern describes. Removing the credit channel makes that contraction deeper, not shallower (tax_base_change_mu -9,539.98 [-12,523.40, -7,849.88] down): mediation and accumulation appear together rather than as alternatives, so the bifurcation as stated is not what the model shows.
- **Evidence.**

  - `outputs/experiments/p10-ablations/runs.parquet` - artifact: ablation citation on M004 - NO_ELITE_CREDIT tax_base_change_mu -9,539.98 [-12,523.40, -7,849.88] down; households_departed +121.18 [+91.79, +400.50] up
  - `outputs/calibration/p09-*/ensemble.parquet` - artifact: calibration citation on M004 - elite_hidden_land_share median 0.359 contraction 0.277; interest_rate_monthly median 0.070 contraction 0.368
  - `outputs/experiments/p10-ablations/runs.parquet` - artifact: counterfactual citation on M004 - JOINT_NO_EXTRACTION_ESCALATION+NO_ELITE_CREDIT closes both channels at once
  - `outputs/calibration/p09-*/predictive_statistics.parquet` - artifact: hold-out citation on M004 - no reserved pattern scores the credit channel
  - `ray-huang-1974` - registry: historical-support citation on M004 - debt-transfers-land (grade B, target, window 1600-1644): calibration window 0.94

### M005 - Insurgent Consolidation (SUPPORTED)

- **Dataset.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913`, `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753`, `outputs/experiments/p10-ablations`, `outputs/experiments/p10-morris`, `outputs/experiments/p12-robustness`
- **Sample size.** `outputs/calibration/p09-cards87a14e9d-32p-ch1-s20260913` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/calibration/p09-cards87a14e9d-32p-ch1-s991753` (32 rows in ensemble.parquet, generated at ab5ca5ca1ffe); `outputs/experiments/p10-ablations` (52 rows in runs.parquet, 240 ticks, 20260914 seed, 13 declared arms, generated at 72aa3f92bc95); `outputs/experiments/p10-morris` (32 rows in runs.parquet, 240 ticks, 20260914 seed, 32 declared arms, generated at 72aa3f92bc95); `outputs/experiments/p12-robustness` (18 rows in levels.parquet, 6 replicates, 240 ticks, 20260916 seed, 4 policy arms, generated at c1497d39a771)
- **Space.** 5 county nodes and 3 external nodes (8 nodes), 25 weighted household cohorts covering 21,000 households, 5 elite houses, 7 merchant houses, 5 county governments, 5 garrison units and 0 armed bands at start; the sandbox is a declared fixture graded S, not a reconstruction
- **Climate.** synthetic stationary: a shock month is drawn independently with probability 0.4 and severity floor 0.6; no observed series and no trend - every batch this card cites ran on it, so no claim in the card is about observed climate
- **Policy arms.** p10-ablations: 13 declared arms, 4 replicates each; p10-morris: morris design; p12-robustness: rule,utility,random,ustc; refused: ustc
- **Extrapolation limits.** The causal link the model names - merging on cohesion - was never exercised: zero merges in every arm of the P10 batch, at every level. Consolidation as an outcome is robust; its attribution inside the model is not, and this card does not claim the outcome and the link together. Five or six replicates per policy is enough to show presence, not to estimate the strength.
- **Counterexample.** The merger arm: raising the cohesion bar to its maximum changes nothing (0 merges against the baseline's 0), and consolidation still appears. Whatever produces it, it is not the merger rule the model's documentation names.
- **Evidence.**

  - `outputs/experiments/p12-robustness/mechanism_readings.parquet` - artifact: policy-robustness citation on M005 - present in rule 0.83, utility 0.83, random 0.83 of replicates; verdict robust
  - `outputs/experiments/p10-ablations/runs.parquet` - artifact: ablation citation on M005 - LOW_REPRESSION largest_band_share_max -0.05 [-0.10, -0.01] down; NO_BAND_MERGER merges 0 against baseline 0
  - `outputs/calibration/p09-*/predictive_checks.parquet` - artifact: hold-out citation on M005 - many-bands-then-consolidation held-out 0.62, whole run 0.94
  - `outputs/experiments/p10-morris/runs.parquet` - artifact: sensitivity citation on M005 - largest band share leads with reference_price_tael_per_shi, food_shi_per_soldier_month, assessed_value_tael_per_mu
  - `lorge-2005` - registry: historical-support citation on M005 - many-bands-then-consolidation (grade B, hold-out, window 1630-1644): held-out years 0.62

### M006 - Famine Mortality (UNIDENTIFIED)

- **Dataset.** none: this card cites the record, not an artifact
- **Sample size.** none
- **Space.** 5 county nodes and 3 external nodes (8 nodes), 25 weighted household cohorts covering 21,000 households, 5 elite houses, 7 merchant houses, 5 county governments, 5 garrison units and 0 armed bands at start; the sandbox is a declared fixture graded S, not a reconstruction
- **Climate.** synthetic stationary: a shock month is drawn independently with probability 0.4 and severity floor 0.6; no observed series and no trend - every batch this card cites ran on it, so no claim in the card is about observed climate
- **Policy arms.** no batch and no policy arm: the card cites the record, and nothing the project ran measures this mechanism
- **Extrapolation limits.** The gap is structural rather than empirical: no mortality event, no mortality parameter, no mortality output, so nothing in the project could have measured this mechanism even in principle. P08 recorded it as a coverage gap; this card records it as the largest single mismatch between the model's mechanism set and the record's causal chain.
- **Counterexample.** The model satisfies land-abandonment-in-famine at 1.00 with zero deaths, so any claim that mortality is necessary for abandonment is contradicted inside the model - and any claim that the model's abandonment is historical evidence for a mortality mechanism is contradicted by the model's own structure.
- **Evidence.**

  - `chen-2024-chongzhen` - registry: historical-support citation on M006 - famine-lags-harvest-failure (grade B, constraint, window 1628-1635): sequence, not a level not scored
  - `shaanxi-famine-gazetteers` - registry: historical-support citation on M006 - land-abandonment-in-famine (grade C, target, window 1630-1644): calibration window 1.00

## 5. Where V1 prose and V1 artifacts disagree

V1 is frozen and is not corrected in place. Each row names the V1 sentence, what the release's own artifacts measure, and what a V2 phase should do about it.

### `docs/mechanisms/cards.yaml` - M001 and M002 uncertainty fields; docs/mechanisms/M001-*, M002-* and index.md; docs/mechanisms/zh/cards.yaml, zh/M001-*, zh/M002-* and zh/index.md; outputs/reports/mechanism-synthesis.md section 5 and its zh translation. M003 states 'three counties and 240 ticks' instead, which is the same county miscount without the cohort sentence.

**Claim.** The mechanism documents state the sandbox the evidence rests on as 'three counties, thirty-two cohorts', in English and in Chinese.

**Measured.** The sandbox those cards cite is the toy fixture: five county nodes plus three external nodes, twenty-five weighted household cohorts. Neither count is three; thirty-two is the P09 particle count, not a cohort count.

**Consequence.** The card's scale statement is wrong in the direction that matters for extrapolation, and the confusion with the particle count means a reader cannot tell which 32 is being quoted. V2-P02 records a space census in the run manifest so this can never again be written from memory; V2-P09 restates the scale from the artifact.

**Evidence.**

- `docs/v2/v1-acceptance-matrix.json` - artifact: census - the measured census: 8 nodes, 5 counties, 25 cohorts, from the built sandbox
- `src/late_ming_lab/networks/fixtures.py` - code: toy_spatial_dataset - five county nodes and three external nodes
- `outputs/experiments/p10-ablations/runs.parquet` - artifact: 52 rows, all from that sandbox - the runs the cards' ablation evidence is read from

### `outputs/reports/mechanism-synthesis.md` - section 5 first bullet; docs/mechanisms/M005-insurgent-consolidation.md

**Claim.** 'Six cards over three counties and thirty-two cohorts' is offered as the limit of the causal claim, and M005 names the merger rule as part of consolidation's chain.

**Measured.** The cards' own ablation evidence records zero band merges in every arm of the batch, including the arm built to prevent them, so no arm exercised the named link.

**Consequence.** The status SUPPORTED rests on the outcome - fewer, larger bands across policies - not on the chain the card narrates. V2-P05 must make the link fire and measure it, or rewrite the chain around formation and dissolution; V1's own counterexample already says so, so this is a mismatch between the card's chain and its evidence rather than a missing finding.

**Evidence.**

- `docs/experiments/ablation.md` - document: the binding table - band_merges 0 in the baseline and 0 in the arm
- `outputs/experiments/p10-ablations/runs.parquet` - artifact: band_merges column - zero merges in every arm's four replicates

## 6. Reproducibility

Every path in this document names a file in the repository, and `verify_audit` resolves all of them. Every sample size in section 4 is read from `docs/v2/baseline-v1.json`, which stores each artifact's own byte hashes, row counts and recorded generation commit; no number here was typed from a report. The statuses in sections 2 and 3 are the phase's judgement and are declared in `src/late_ming_lab/release/audit.py`; changing one changes this document's content digest. Nothing in this audit re-runs the model, reads a licensed source, or writes inside a V1 artifact directory.
