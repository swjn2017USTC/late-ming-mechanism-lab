"""The V1 release-candidate freeze and the V2-P00 scientific audit.

``baseline`` freezes and verifies what ``v0.1.0-rc1`` is, by content hash; ``audit`` records what
that release does and does not support, and reads every number it quotes out of the artifacts.
"""
